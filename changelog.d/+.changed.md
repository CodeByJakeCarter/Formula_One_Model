Added initial `DriverCreate`, `DriverRead`, and `DriverUpdate` Pydantic schemas for driver API contracts.
Added SQLAlchemy as a runtime dependency for upcoming ORM model implementation.
Added initial SQLAlchemy `Driver` ORM model with uniqueness constraints and audit timestamps.
Added initial SQLAlchemy engine and session factory setup for database access.
Added `DriverRepository` methods for lookup, create, list, and delete operations.
Added initial `DriverService` layer that composes repository access behind a service object.
Expanded `DriverService` with get, list, and delete operations with transaction handling.
Added `get_db()` dependency for request-scoped DB sessions with rollback-on-exception and close-on-exit behavior.
Added initial `/api/v1/drivers` API router with a create endpoint wired through the service layer.
