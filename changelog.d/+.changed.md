Expanded `DriverService` with get, list, and delete operations with transaction handling.
Updated driver create endpoint to return HTTP 201 Created.
Switched API route handlers and DB dependency to async signatures and updated tests to use `httpx.AsyncClient` with `ASGITransport`.
Resolved Q-001 by selecting Jolpica as canonical source and unblocked REQ-001 in task-engine bookkeeping.
